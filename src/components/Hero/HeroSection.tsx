import HeroContent from './HeroContent';

const HeroSection = () => {
  return (
    <section className='font-roboto pb-22.5'>
      <HeroContent />
    </section>
  );
};

export default HeroSection;
